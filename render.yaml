services:
  - type: web
    name: tywaky-backend
    runtime: node
    plan: free
    region: frankfurt # Or close to user
    rootDir: server
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: MONGODB_URI
        value: "mongodb+srv://tywakysocial_db_user:aSJqJYD5AVw1Tiy8@tywaky-db.vzzw1fg.mongodb.net/?appName=tywaky-db"
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        value: "*"
